<h2>Communities</h2>

	<ul class="communities">
		<% @user_communities.each do |c| %>
			<li class="col-sm-3 col-md-2">
				<%= link_to c.title, collections.collection_path(c) %>
			</li>
		<% end %>
	</ul>

