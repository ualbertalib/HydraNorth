<h2>Descriptions</h2>
<dl class="dl-horizontal file-show-term file-show-descriptions">
  <% present_terms(@presenter,@presenter.terms - [:title, :date_submitted]) do |r, term| %>
  <dt><%= r.display_label(term) %></dt>
      <dd><%= r.value(term) %></dd>
  <% end %>
</dl>
